import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(theme => ({
    root: {
        maxWidth: "800px",
        width: "100%",
        marginTop: "5px"
      } 
}))

export {useStyles}